# AGENTS.md —— Qoder 项目上下文

## 项目简介

本项目是一个面向青少年的 AI 心理健康平台，参加 AIC 算法大赛。平台通过多模态情绪感知、心理评估、智能干预和危机升级四大核心层，为青少年提供安全、可信的心理健康服务。

## 技术栈

- **语言**：Python 3.10+
- **Web 框架**：FastAPI
- **深度学习**：PyTorch、HuggingFace Transformers
- **音频处理**：librosa
- **传统 ML**：XGBoost、scikit-learn
- **联邦学习**：Flower (flwr)
- **端侧推理**：TFLite

## 目录结构

```
backend/
├── perception/          # 感知层 —— 多模态情绪感知（文本/语音/面部/行为/融合）
├── assessment/          # 评估层 —— 心理状态评估（量表/风险/趋势/报告）
├── intervention/        # 干预层 —— AI 心理干预（CBT/苏格拉底/危机/策略）
├── escalation/          # 升级层 —— 危机升级与转介（检测/自伤评估/协议/转介）
├── audit/               # 公平性审计 —— 偏差检测/公平性指标/可解释性
├── privacy/             # 隐私架构 —— 数据脱敏/知情同意/加密存储
├── federated/           # 联邦学习 —— 客户端训练/聚合策略/通信协议
├── shared/              # 共享数据类 —— 跨模块统一数据接口
│   └── dataclasses.py   # 所有数据类定义（EmotionResult/RiskAssessment/CrisisAlert 等）
├── api/                 # API 路由层 —— FastAPI 端点注册
├── tests/               # 单元测试
└── main.py              # 服务入口
```

## 编码规范

### 数据类与模块通信

- 所有数据类使用 `@dataclass` 实现，统一定义在 `shared/dataclasses.py`
- 所有模块间通信必须通过 `shared/dataclasses.py` 中的共享数据类，禁止各模块自行定义数据结构
- 新增数据类时同步更新 `shared/__init__.py` 的导出列表

### 随机种子

- 所有涉及随机性的操作（模型初始化、数据划分等）固定种子为 `42`，确保结果可复现

### 密钥管理

- API Key 等敏感信息一律从环境变量读取（`os.environ` 或 `pydantic-settings`），禁止硬编码

### 危机干预代码标注

- 所有涉及危机干预逻辑的代码，必须在函数或代码块上方添加注释：
  ```python
  # ⚠️ 比赛 Demo 为模拟实现，生产环境需接入真实危机干预流程
  ```

### 通用规范

- 类型注解：所有函数签名必须包含完整的类型注解
- 文档字符串：公共函数和类使用中文 docstring
- 测试覆盖：`shared/dataclasses.py` 的每次修改必须伴随单元测试

## 安全红线

以下规则为强制性约束，任何代码生成均不得违反：

### 禁止诊断性语言

- 禁止生成任何形式的诊断性表述，如"你有抑郁症""你患有焦虑症"
- 可使用"检测到焦虑情绪较为明显"等非诊断性描述

### 禁止药物建议

- 禁止提供任何药物名称、剂量建议或用药指导
- 涉及药物相关问题时，统一引导至专业医疗机构

### 高风险必须触发升级

- 当 `RiskAssessment.risk_level` 为 `high` 或 `crisis` 时，必须触发 `escalation` 升级流程
- 不允许在高风险状态下仅返回普通干预结果而跳过升级

### 人工审核

- 所有安全相关规则的变更必须经人工审核后方可合入
- 自动化流程不得自行修改安全阈值或升级条件
